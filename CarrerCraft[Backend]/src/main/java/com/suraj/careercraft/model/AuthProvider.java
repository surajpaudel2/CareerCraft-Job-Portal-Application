package com.suraj.careercraft.model;

public enum AuthProvider {
    LOCAL,    // For form-based login (username/password)
    GOOGLE,   // For OAuth via Google
    GITHUB,   // For OAuth via GitHub
    FACEBOOK  // For OAuth via Facebook
}
